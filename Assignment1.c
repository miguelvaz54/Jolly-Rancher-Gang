#include <stdio.h>
#include <math.h>

int main(int argc, char **argv) {
    //The main function encapsulates and calls the other functions; returns 0
    calculateDistance();
    calculatePerimeter();
    calculateArea():
    calculateWidth();
    calculateHeight();
}